#include <main.h>
#include <holberton.h>

/**
 * print_alhpabet_x10 - repeats the print_alphabet 10 times.
 * Return: Always 0.
 */
void
{
	print_alphabet_x10(void);
	return (0);
}
{ 
	int i;
	char c;
	for (1 - 0; i < 10; ++i)
	{
		for (c = 'a'; c <= 'z'; c++ )
		{ 
		    _putchar(c);
		 }
		_putchar('\n');
	}

}
